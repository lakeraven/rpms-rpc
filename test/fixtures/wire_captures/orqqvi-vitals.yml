# Wire capture: ORQQVI VITALS — generated by `rake wire:capture` (issue #189).
# raw_return is VERBATIM from the bcer-9.0-ydb broker (synthetic data only); do not hand-edit — re-run the capture.
# Gated by test/rpms_rpc/wire_contract_test.rb against mapping :vitals.
---
rpc: ORQQVI VITALS
mapping: vitals
kind: fields
release_tag: bcer-9.0-ydb
captured_at: '2026-09-03T01:19:43Z'
cite: 'FASTVIT^ORQQVI (bcer-9.0-ydb r/ORQQVI.m:61-84): the #8994 registry sends
  "ORQQVI VITALS" to FASTVIT, not VITALS (.broker_dumps_8994_20260607.txt:565
  "ORQQVI VITALS^FASTVIT^ORQQVI"). FASTVIT''s own header reads "vital measurement
  ien^vital type^rate^date/time taken" — rate at piece 3, datetime at piece 4, the
  REVERSE of VITALS^ORQQVI (see :vitals_for_date_range). Row construction:
  VITAL^ORQQVI (VA/GMRV branch) and MSR^ORQQVI (IHS V MEASUREMENT branch, taken
  when DUZ("AG")="I" — r/ORQQVI.m:96,167-186) both emit
  IEN_U_ABBREV_U_VALUE_U_$P(DATA(1),U); pieces 5/6 are the display value and its
  metric conversion, piece 7 the qualifiers ($P(ORY(CNT),U,7)=QUALS(CNT)).
  Newest-per-type only: each per-type walk ends on `Q:OK`.'
note: 'Corrected twice. Shipped as TYPE^VALUE^UNITS^DATE (invented). The first fix
  read a plausibly-named tag (VITALS^ORQQVI) and declared IEN^TYPE^DATETIME^value —
  right shape, wrong RPC: that is "ORQQVI VITALS FOR DATE RANGE" (dump line 795).
  Resolve the registry name->tag row FIRST, then read that tag.'
pieces:
- position: 0
  attributes:
  - measurement_ien
  fileman_type: integer
- position: 1
  attributes:
  - type
- position: 2
  attributes:
  - value
  - rate
- position: 3
  attributes:
  - recorded_date
  - recorded_datetime
  fileman_type: fileman_datetime
- position: 4
  attributes:
  - display
- position: 5
  attributes:
  - metric_display
- position: 6
  attributes:
  - qualifiers
source: no-data
# Relabelled from live-capture: the call was real, but the reply carried no
# data rows (bcer-9.0-ydb had nothing seeded for this patient), so the pieces
# above are a source cite, not evidence. Re-run `rake wire:capture` against a
# box with seeded clinical data to promote this back to live-capture — and note
# that a one-row reply on an IHS box is the shipped-routine CNT-by-value bug
# (r/ORQQVI.m:96), not a patient with one vital.
inputs:
- '8'
- '2900101'
- '3991231'
raw_return: "4\0"
sha256: 3089c464221340fed4229d6ba913a04224c91b44158c22163e8849eb7dcc4878
