# Wire capture: DDR GETS ENTRY DATA — generated by `rake wire:capture` (issue #189).
# Routine-cited shape (no live call — see cite); do not hand-edit raw claims.
# Gated by test/rpms_rpc/wire_contract_test.rb against mapping :ddr_gets_entry_data.
---
rpc: DDR GETS ENTRY DATA
mapping: ddr_gets_entry_data
kind: text_blob
release_tag: bcer-9.0-ydb
captured_at: '2026-09-03T01:22:57Z'
cite: 'GETSC^DDR2 (DDR2.m:22-43,61): ''[Data]'' then FILE^IEN^FIELD^INTERNAL^EXTERNAL
  rows; ''[ERROR]'' block on failure'
note: text_blob mapping — the gate checks reply-kind consistency and records the real
  reply grammar; row parsing lives in DdrFileman.gets_entry.
pieces: []
source: routine-cite
